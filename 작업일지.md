# 사직스코어 작업일지

집·회사에서 **이 파일만** 보고 이어서 작업한다.  
최신 내용이 위에 온다. 작업이 끝나면 맨 위에 날짜 칸을 추가하고 `git push` 한다.

```powershell
git pull origin main
# 이 파일 수정
git add 작업일지.md
git commit -m "작업일지: (한 줄 요약)"
git push origin HEAD
```

버전은 `app/build.gradle.kts`만 올린다. APK·`.gradle`·`app/build`는 커밋하지 않는다.

---

## 현재 상태 (한눈에)

| 항목 | 값 |
|------|-----|
| 버전 | **1.3.77** / `versionCode` **1085** |
| 브랜치 | `main` |
| 백업 | `backup/pre-redesign-1.3.27` (레이아웃 갈아엎기 직전, 1.3.27) |
| 실기 | S26 울트라(`R5KL10FHTTN`)에 **1.3.77** debug 설치 |
| 빌드 | `.\scripts\env.ps1` → `.\gradlew.bat :app:assembleDebug` (JDK 17 자동 탐색) |

---

## 2026-09-04 (회사) 1.3.77

### 한 일

1. **위젯** — 새로고침 누르면 금색. 컴팩트는 순위·로고를 가운데 크게.
2. **등말소 새벽 재알림** — 날짜만 바뀌거나 선수코드가 비었다 채워져도 같은 공시는 다시 안 울림.
3. **경기 전 알림** — 원정/홈·구장 표시. 같은 내용이면 프로세스 넘어가도 다시 안 그림.
4. **라이브 다이아** — 52dp.
5. **점수만** — 로고와 점수만 있는 스코어카드.
6. **라이브 바** — 상세 알림과 같은 스코어카드.

---

## 2026-09-03 (회사) 1.3.76

### 한 일

1. **위젯 새로고침이 안 눌리던 문제** — Glance `ActionCallback`이 삼성 홈에서 씹히고, 본문 클릭과 겹쳤다. 버튼을 오른쪽 위 오버레이로 두고 위젯 리시버 브로드캐스트로 강제 fetch.
2. **내용이 아래로 쏠리던 문제** — 새로고침 전용 줄이 본문을 밀고, 컴팩트 헤더 아래 `defaultWeight` 빈칸이 스코어를 내렸다. 버튼을 겹치고 점수는 제목 바로 아래로.

---

## 2026-09-03 (회사) 1.3.75

### 한 일

1. **라이브 히어로가 어제 결과** — 위젯만 날짜를 걸렀고 라이브 탭은 `lotteGame`을 그대로 썼다. 오전 5시 기준 오늘이 아니면 다음 예정으로 넘기고 스냅샷을 다시 받는다.
2. **히어로 디자인** — 앞선 점수 금색, 라이트 모드 빨간 그라데이션을 되돌림. 테마 칩은 바로 선택되고 상태바 아이콘도 테마를 따라간다.

---

## 2026-09-03 (회사) 1.3.74

### 한 일

1. **어제 결과가 위젯·라이브에 남던 문제** — `kboToday()`는 오전 5시 경계인데, 스냅샷이 어제 종료 경기를 계속 `lotteGame`으로 들고 있었다. 5시 이후 스냅샷은 버리고, 일정 API가 섞어 준 전날 경기도 걸러 낸다.
2. **위젯 초기화** — 앱 내부 업데이트(`MY_PACKAGE_REPLACED`)와 매일 05:00 알람에서 스냅샷을 강제 갱신한 뒤 위젯을 다시 그린다.

### 확인함

- 단위 테스트·debug 빌드.

---

## 2026-09-03 (회사) 1.3.73

### 한 일

1. **Now Bar = Ongoing Activity?** — 네이버지도 공지는 삼성 One UI 7 파트너 기능. 공개 SDK 없음, 패키지 화이트리스트.
2. S26은 One UI 8 Live Update가 Now Bar 경로. **라이브 바**를 커스텀 스코어카드에서 `ProgressStyle` + `setRequestPromotedOngoing`으로 되돌림. 상세 알림 카드는 유지.
3. 네이버지도와 같은 `com.samsung.android.support.ongoing_activity` 메타데이터 + `android.ongoingActivityNoti.*` extras를 칩에 붙임 (화이트리스트 없이는 무시될 수 있음).
4. Now Bar 채널 `live_score_nowbar_v2`를 만들자마자 지우던 버그 수정.

### 확인함

- 단위 테스트·debug 빌드. 칩 실기는 경기 중 + 설정에서 「라이브 바」.

---

## 2026-09-03 (회사) 1.3.72

### 한 일

1. **자동 업데이트 속도** — 앱 프로세스 시작과 동시에 GitHub를 본다. 릴리스 본문 `versionCode`로 최신 여부를 판단해 이미 최신이면 `update.json`을 받지 않는다.
2. 비공개 APK는 API 에셋+토큰으로 받는다. 「확인 중」 창은 없애고, 받을 때만 진행률.
3. 앱을 다시 켜면 45초 뒤부터 재확인.

### 확인함

- 단위 테스트·debug 빌드. S26 설치.

---

## 2026-09-03 (회사) 1.3.71

### 한 일

1. **위젯 새로고침** — 전체 영역 클릭이 버튼을 가로채던 문제를 고침. 누르면 강제 fetch.
2. **새로고침 실패 배너** — 캐시가 있으면 자동 폴링 실패로 다시 띄우지 않음. 닫아도 10초마다 돌아오던 원인.
3. **승률 1:99** — 3회 2점차처럼 초반 API 극단값을 점수 추정 근처로 보정.
4. **알림 배치** — 빅이닝식 로고–점수–다이아–점수–로고 / 투수·BSO·타자 / 승률 바.

### 확인함

- 단위 테스트·debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사) 1.3.70

### 한 일

1. **「새로고침 실패 · 마지막 HH:mm」** — 히스토리·엔트리가 아니라 라이브 점수 자동 새로고침 안내임을 분리. 닫기 추가.

---

## 2026-09-02 (회사) 1.3.66~1.3.69

### 한 일

1. **1.3.66** — 라이브 바·점수만도 스코어카드. `CATEGORY_PROGRESS`가 ProgressStyle로 덮이던 문제.
2. **1.3.67** — 런처 아이콘을 SAJIK 구장 배지로 교체.
3. **1.3.68** — Now Bar 깜빡임: 경기 전 FGS 반복 시작 제거, 동일 내용 notify 생략.
4. **1.3.69** — 상태표시줄 야구공 실루엣, 잠금화면 `publicVersion`·칩 문구.

---

## 2026-09-02 (회사) 1.3.65

### 한 일

1. **상세 알림 다시 표시** — FGS가 메인 스레드에서 로고를 받으며 알림을 지우던 문제 수정. 경기 전에는 알림만 고정.
2. **라이브 바·점수만** — 스코어카드 디자인 통일 (캐시 로고만 사용).

### 확인함

- 단위 테스트·debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사) 1.3.64

### 한 일

1. **다시 표시** — lead 창 밖 경기 전에도 알림 고정(pinned). FAST_POLL이 서비스를 켜며 알림을 지우던 문제 수정.
2. **라이브 바** — 옛 이닝 진행 바(ProgressStyle) 대신 스코어카드 디자인 사용.

### 확인함

- 단위 테스트·debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사) 1.3.63

### 한 일

1. **앱 안 열면 알림 안 오던 문제** — WorkManager(15분)만 기대하던 구조를 고침. `AlertWatchService` 포그라운드 20초 폴링, 부팅 시 `AlertBootstrap` 즉시 1회 + fast poll 예약, `ROSTER_POLL` 매니페스트 등록, WakeLock.
2. **30분 늦음** — 삼성 배터리 최적화에서 Alarm이 끊겨도 FGS가 KBO GetRoster·라인업을 직접 본다.
3. **README** — 엔트리·라인업 3중 백업 구조 문서화.

### 확인함

- 단위 테스트·debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사) 1.3.62

### 한 일

1. **엔트리·라인업 알림 속도** — KBO 공식 GetRoster 경량 폴링(30초, 08~23시). 라인업 fast poll 3분→20초(경기 6시간 전~). 경기 전 LiveScoreService 20초→8초.
2. **README** — 버전 1.3.62 반영.

### 확인함

- 단위 테스트·debug 빌드 후 S26 울트라 설치.

---

## 2026-09-02 (회사) 1.3.61

### 한 일

1. **승리확률 0:0 버그** — 네이버 `1`/`99` 퍼센트를 100%/99%로 잘못 읽던 파싱 수정. 0:0이어도 40:60 같은 값은 유지. 네이버 중계 → 루타 → 추정 순으로 소스 선택.
2. **hotColdZone** — 프리뷰 로딩 전 빈 스냅샷이 고정되던 것을 이전 값 유지로 수정.
3. **네트워크 실패 UX** — 라이브 화면 오류 줄에 `다시 시도` 버튼.
4. **README** — 버전 표를 1.3.61에 맞춤.
5. **단위 테스트** — `WinProbTest` 추가.

### 확인함

- (빌드·실기 설치 예정)

---

## 2026-09-02 (회사) 1.3.60

### 한 일

1. **실시간 스코어 알림 다시 표시** — 표시 시작 시간과 무관하게 오늘·다음 경기 알림을 바로 띄움.
2. **Now Bar 설정 버튼** — 문구 정리, 열리면 라이브 알림 경로 토스트 안내.

### 확인함

- debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사) 1.3.59

### 한 일

1. **Now Bar 표시 시작** — 최대 4시간, 30분 단위 칩 8개로 고침 (슬라이더 3시간 안 잡히던 문제).
2. **설정 문구** — 표시 시작 아래 설명 줄임.
3. **알림 프리셋** — 무음 시간 UI 제거.

### 확인함

- 단위 테스트·debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사) 1.3.58

### 한 일

1. **Now Bar 시작** — 설정에서 경기 시작 몇 분 전부터 띄울지 고름 (30분 단위, 기본 2시간). 아침부터 하루 종일 안 뜸.
2. **종료 후** — 결과 카드는 남기되 `ongoing`을 떼서 밀어 지울 수 있게. 다음 경기 카드로 안 바뀜.
3. **펼친 알림** — 루상 다이아몬드·BSO 점을 키움.

### 확인함

- 단위 테스트·debug 빌드 후 S26 울트라 설치.

---

## 2026-09-01 (회사) 1.3.57

### 한 일

알림을 누르면 메인만 열리던 것을 고침. 득점→중계, 라인업→라인업 탭, 엔트리 등말소→엔트리 화면. `sajik://` 딥링크로 PendingIntent extras가 비는 것도 막음.

### 확인함

- 단위 테스트·debug 빌드 후 S26 울트라 설치.

---

## 2026-09-01 (회사) 1.3.56

### 한 일

펼친 상세 알림이 밋밋하고 답답했다. 네이비 박스는 다시 안 넣고, 배치·대비만 손봤다.

1. **배치** — 이닝 알약을 점수 사이에서 위로. 가운데는 `3 : 2`. 루상+BSO는 한 줄.
2. **색** — 빈 루·BSO 점이 네이비용 흰색이라 시스템 알림에서 묻히던 걸 중간 회색으로. 알약은 라이브=초록 / 예정·중단=골드 / 종료=회색 / 취소=레드. 승률 바 끝을 둥글게.

### 확인함

- debug 빌드 후 S26 울트라 설치. 알림 펼쳐서 확인.

---

## 2026-09-01 (회사) 1.3.55

### 한 일

1. **우천중단** — 취소와 구분. `우천중단 (19:50 예정)`처럼 사유+재개 시각. 상태는 LIVE 유지(점수·폴링·알림 계속).
2. **라이브 히어로** — 빅이닝 때 넣었던 흰 카드·팀별 박스를 빼서 한 덩어리로.

### 확인함

- 단위 테스트·debug 빌드 후 S26 울트라 설치.

---

## 2026-09-01 (회사) README

`README.md`를 1.3.54 기준으로 맞춤. 라이브 히어로·위젯 순위·Now Bar 조건·`env.ps1`/`L:\` 빌드. 버전 코드는 그대로.

---

## 2026-09-01 (회사) 1.3.54

### 한 일

위젯 하단이 `7위 · 잔여 31 · 선발 로드리게스 · 8...`로 잘렸다. 순위는 각 로고 위, 선발은 그 위 줄만, 하단은 `잔여 31`만.

### 확인함

- S26 울트라에 1.3.54 debug 설치. 위젯 새로고침 후 확인.

---

## 2026-09-01 (회사) 1.3.53

### 한 일

1. **위젯** — 1.3.38 취소 전용 레이아웃을 빼고 그 직전(1.3.37)으로 되돌림. 캐시가 비어 있으면 한 번 fetch.
2. **폴링** — 서비스가 `force=true`로 앱과 같이 받아 가던 것을 `force=false` + 8초 캐시 재사용.
3. **README** — Now Bar 칩은 라이브 바·점수만. 상세 알림은 승격 안 됨.

하단 탭 여백 80dp는 안 넣음. Scaffold가 이미 bottomBar 높이를 뺀다.

### 확인함

- S26 울트라에 1.3.53 debug 설치 (`adb install -r`). 서명 충돌 없음.

---

## 2026-08-31 (회사) 1.3.52

### 한 일

[빅이닝](https://blog.naver.com/lifebystride/224316526720) 라이브 카드 느낌을 가져왔다. 기능은 그대로 두고 비주얼만.

1. **라이브 히어로** — 흰 둥근 카드, 초록 LIVE / WIN·LOSE·DRAW, 가운데 `0 : 0`, 초록 이닝 알약, 팀 카드(로고·이름·선발), 경기 중 다이아몬드+BSO+투수/타자.
2. **오늘 경기 줄** — 가로 칩이 아니라 카드(LIVE 알약, 큰 점수).
3. **일정 캘린더** — 예정 홈=파랑, 원정=빨강.

마스코트 일러스트·응원 하트는 안 넣음. 공식 로고를 쓴다.

### 확인함

- `:app:assembleDebug` 성공. ADB에 기기 없음. 연결되면 `adb install -r L:\app\build\outputs\apk\debug\app-debug.apk`

---

## 2026-08-31 (회사) 1.3.51

### 한 일

하단 탭에서 선택된 페이지가 점만 보여서 이름을 알기 어려웠다. 라이브·결과·순위·설정 **항상 글자로 표시**, 선택된 탭은 볼드·롯데 레드.

### 확인함

- S26 울트라에 1.3.51 설치. 기존 1.3.49와 서명이 달라 앱을 지운 뒤 다시 깔음.

---

## 2026-08-31 (회사) 1.3.50

### 한 일

1. **다시 표시가 예전 ProgressStyle로 뜨던 것** — 오늘 경기가 없으면 `lotteGame`이 null이라 점선 바로 떨어졌다. 다음 경기를 쓰고, 스냅샷을 먼저 받은 뒤 카드를 올린다. 서비스를 껐다 켜서 기본 알림이 깜빡이지 않게 같은 ID로 덮는다.
2. **승률 게이지** — 펼친 카드 하단에 `LG 42%` / `58% 롯데`와 색 바. 어두운 팀색은 알림 배경에 묻히지 않게 밝힘. 취소만 숨김.
3. **매직·트래직 알림 사유** — `12 → 11 · 롯데, 두산 5:3 승 · KT, 키움 2:4 패` 처럼 당일·어제 경기 결과를 본문에 넣음.

### 확인함

- MagicNumber 단위 테스트 통과 (사유 문구 포함)
- 릴리스/실기 설치는 다음에

---

## 2026-08-30 (집) 1.3.49 — 알림 네이비 박스 제거

### 한 일

네이비 배경을 빼면 시스템 알림 회색과 한 덩어리가 된다. 로고·점수·상태 알약은 그대로 두고, 글자색은 `TextAppearance.Compat.Notification`로 다크/라이트 셰이드를 따른다. colorized는 끈다(작은 아이콘만 롯데 레드).

### 확인함

- 접힌 알림: 다른 시스템 알림과 같은 회색 칸. 로고·점수·`취소(우천)` 알약. 네이비 박스 없음
- 펼친 알림: 헤더·내용이 한 색. `구장 사직` 보임. 글자 대비 정상

---

## 2026-08-30 (집) 1.3.48 — 알림 전체 색

### 한 일

작은/큰 알림 모두 바깥 회색이 남는 이유를 재확인했다. 안쪽 네이비는 우리 RemoteViews 배경이고, 왼쪽 아이콘·오른쪽 펼치기·헤더 회색은 One UI가 Android 12+ 표준 템플릿으로 강제하는 칸이다. `setColorized(true)`는 포그라운드 서비스이거나 MediaStyle+세션일 때만 시스템 배경을 칠하는데, 종료·취소는 서비스를 떼서 색이 무시되고, MediaStyle을 붙이면 스코어보드가 미디어 알림(`이 휴대전화`)으로 바뀌어 못 쓴다. 투명 배경은 이전에 글자 대비가 깨져 접었다. **네이비 스코어보드 박스 유지.** 앱에서 아이콘 칸까지 칠할 방법은 없다.

---

## 2026-08-30 (집) 1.3.47 — 경기 전 알림도 같은 카드

### 한 일

`실시간 스코어 알림 다시 표시`를 누르면 경기 전이 예전 ProgressStyle(점수 텍스트 + 점선 바)로 나왔다. 상세 알림 카드가 라이브·종료에만 붙고 `BEFORE`는 빠져 있었기 때문.

1. 상세 알림(FULL)이면 경기 전·중·종료·취소 모두 네이비 카드.
2. 경기 전 카드: 가운데 알약에 시작 시각(`18:00`), 펼치면 양 선발, 접히면 구장. 승률 바는 숨김.
3. 다시 표시 직후 빈 알림이 잠깐 예전 디자인으로 뜨지 않게, 서비스가 캐시된 스냅샷으로 바로 카드를 올린다.

### 확인함

- `dumpsys`: 채널 `live_score_card_v3`, 색 `#0B2A4A` (예전 ProgressStyle 채널이 아님)
- 접힌 카드: 로고·점수·`취소(우천)` 알약 (오늘 경기는 우천 취소라 경기 전 대신 같은 카드 경로로 확인)
- 펼친 카드: 다이아몬드·BSO 없음, 구장만. 예전 `롯데 0:0 / 18:00 두 줄 / 구장 사직...` 점선 바는 사라짐

---

## 2026-08-30 (집) 1.3.46 — 알림 표시 영역


### 한 일

알림이 "일부 잘려 보인다"는 지적을 좌표로 재봤다. **좌측 여백은 One UI가 모든 알림에 잡는 앱 아이콘 칸**이고(다른 앱 알림도 텍스트가 같은 x=326px에서 시작), 우리 카드는 그 오른쪽 영역을 이미 100% 쓰고 있었다. 앱에서 더 넓힐 방법은 없다. 대신 **내용이 잘리는 것**을 고쳤다.

1. **접힌 알림 전용 한 줄 레이아웃** (`notification_live_compact.xml`) — 접힌 알림은 높이가 64dp쯤에서 잘려서 큰 카드를 그대로 넣으면 중간이 썰렸다. 로고·점수·상태 알약(·경기 중이면 루상·아웃·타자)만 한 줄로. 큰 카드는 펼친 상태·`BigContentView`에만.
2. **투수/타자 이름 안 잘리게** — `투수 케네... / 타자 3번 ...`으로 잘렸다. BSO를 가운데 독립 줄로 올리고 투수·타자에 각각 절반 폭을 줬다. 이제 `승 카라스코` / `패 김진욱`이 다 보인다.
3. 종료 상태 접힌 줄에서는 승패 문구를 빼서 잘림 없음.

### 확인함

- 접힌 알림: `LG 8 [종료] 3 롯데` 한 줄로 잘림 없음
- 펼친 카드: 다이아몬드·BSO 없이 `승/패` 전체 표시
- 카드 폭 측정(1440px 기준): 알림 컨테이너 64~1376, 앱 아이콘 칸 122~250, 우리 카드 320~1340 → **가용 영역 전체 사용**

### 다음에

- [x] `setColorized(true)`는 One UI 9에서 컨테이너 배경까지 칠하지 않는다(회색 여백 그대로). 배경 투명 + 시스템 텍스트색 시안을 기기에 띄워 비교했고, **네이비 스코어보드 박스를 유지하기로 결정**했다. 투명은 폭이 꽉 차 보이는 대신 점수판 느낌이 사라지고, `?android:attr/textColorPrimary`가 알림 셰이드 테마를 못 따라와 글자가 거의 안 보였다(직접 색 리소스에 `values-night`를 두면 되지만 이득이 없었다)
- [ ] 경기 중 카드(투구수·루상·BSO·타자)는 다음 경기 때 실기 확인

---

## 2026-08-30 (집) 1.3.45 — 1.3.39~1.3.44 재점검

### 한 일

집에서 1.3.39부터 1.3.44까지 몰아 넣은 것들을 다시 보니 **모드가 겹치고 알림이 안 지워지는** 문제가 있어서 정리했다.

1. **표시 모드 3개가 실제로 달라짐** — 1.3.40에서 `useCustom = mode != STATUS_SCORE`로 넣어 「라이브 바」를 골라도 커스텀 카드가 덮어써서 Now Bar 칩이 죽어 있었다. `mode == FULL`일 때만 카드를 쓴다.
   - 라이브 바(LOCK_NOW) = ProgressStyle + 칩 승격
   - 상세 알림(FULL) = 스코어카드 (칩 승격 불가, 설정 설명에 명시)
   - 점수만(STATUS_SCORE) = 한 줄 + 칩
2. **끝난 경기 알림이 안 없어지던 것** — 서비스는 종료 3초 뒤 멈추는데 알림은 `setOngoing(true)`라 `NO_CLEAR`가 붙어 손으로도 못 지웠다. 종료·취소면 `setOngoing(false)` + `setAutoCancel` + **2시간 후 자동 만료**, 서비스는 `STOP_FOREGROUND_DETACH` 후 한 번 더 `notify`해서 `NO_CLEAR`를 뗀다. (dumpsys 확인: `flags=ONLY_ALERT_ONCE|AUTO_CANCEL|SILENT`)
3. **새벽에 어제 경기 카드가 다시 뜨던 것** — `GiantsApp`이 앱 열 때마다 ENDED 카드를 다시 올렸다. **오늘 날짜 경기만** 올린다.
4. **종료 카드 내용** — 뜻 없는 빈 다이아몬드·BSO를 아예 숨기고 그 줄에 `승 카라스코` / `패 김진욱`(세이브 있으면 `· 세 OOO`). 사용자가 지적한 "주자 없는데 1·2루처럼 보임"의 뿌리.
5. **요약 탭 주요 장면 `0회`** — KBO `tableEtc`(결승타·홈런)는 이닝을 안 줘서 전부 `0회`로 찍혔다. 이닝이 없으면 칸을 비운다.
6. **라이브 탭 접힘 완성** — 1.3.44의 `LazyColumn` + `stickyHeader`에 더해, 탭 내용이 짧아도 한 화면을 채우도록 최소 높이를 준다(뷰포트 − 44dp). 프리뷰처럼 내용이 적은 탭에서도 상단이 접힌다.
7. **`scripts\env.ps1`** — JDK 17 경로가 하드코딩(`17.0.20.8`)돼 집 PC(`17.0.19.10`)에서 빌드가 안 됐다. Microsoft/Adoptium/Java/Studio jbr에서 자동 탐색.
8. 안 쓰는 `notification_live_big.xml` 삭제.

### 확인함 (S26 울트라, ADB)

- 릴리스 빌드 성공 → 설치 → 실기 캡처로 확인
- 라이브 탭: 프리뷰·라인업·요약·중계·기록 전부 탭 누르면 히어로·퀵링크가 접히고 탭 줄이 상단 고정, 아래가 한 화면. 중계 문자중계(콜드게임 문구까지)·기록 라인스코어 정상
- 요약 탭 주요 장면에 `0회` 사라짐 (`결승타: 박해민(2회 2사 만루서 밀어내기 4구)`)
- 알림: 상세 알림 모드 → 네이비 카드, 다이아몬드·BSO 없음, `승/패` 표시, **스와이프로 지워짐**
- 표시 모드 칩 전환 시 설명 문구가 모드별로 맞게 바뀜

### 남은 것 / 다음에

- [ ] Now Bar 칩은 **경기 중 라이브 바 모드**에서만 확인 가능 (종료 경기에선 승격 안 함). 다음 경기 때 확인
- [ ] 알림 카드 좌우 여백은 One UI가 잡는 앱 아이콘·확장 화살표 자리라 앱에서 더 못 없앰. colorized로 카드가 꽉 찬 것처럼 보이게 한 게 한계
- [ ] 워치4 타일·컴플리케이션 실기

---

## 2026-08-29 ~ 08-30 (집) 1.3.38 ~ 1.3.44

집에서 이어서 한 작업. 하나씩 커밋했고 내용은 아래와 같다. (재점검 결과는 위 1.3.45 칸)

| 버전 | 한 일 |
|------|------|
| 1.3.38 / 1045 | 취소 경기에 30분 전 알림·위젯 `0:0` 스코어보드가 뜨지 않게. 취소 판별 강화 + 예약 알람 해제, 위젯은 취소 라벨만 |
| 1.3.39 / 1046 | 상세 알림을 **로고·승률바 카드**로. 요약 승률(네이버 한쪽만 와도 보정 + 이전 스냅샷 누적), 경기 90분 전부터 폴링·3분 검사로 라인업·취소·등말소 지연 단축 |
| 1.3.40 / 1047 | 실시간 알림을 점수판 카드로 강제, 투수 **투구수** 표시 → *이때 라이브 바 모드가 카드에 덮였다 (1.3.45에서 고침)* |
| 1.3.41 / 1048 | 라이브 탭 히스토리·엔트리·타이틀을 아이콘 카드로 (텍스트만인 링크 제거) |
| 1.3.42 / 1049 | 스코어카드가 알림창 가로를 채우게 `DecoratedCustomViewStyle` 제거, 「칩 숨기기」 버튼 삭제 → *이때 지울 방법이 사라졌다 (1.3.45에서 고침)* |
| 1.3.43 / 1050 | 종료 경기 알림에서 남은 주자·볼카운트 제거(`liveSituation`), 빈 루 드로어블은 테두리만. 라이브 탭 스크롤 시 상단 접힘 1차 |
| 1.3.44 / 1051 | 스코어카드 채널을 `live_score_card_v3`(DEFAULT)로 올려 `setColorized`가 먹게. 라이브 상세를 `LazyColumn` + `stickyHeader`로 |

### 이때 배운 것

- `setColorized(true)`는 채널 중요도가 **DEFAULT 이상**이어야 먹는다. LOW 채널이면 조용히 무시된다
- 채널 중요도는 만든 뒤 코드로 못 바꾼다 → ID에 `_v3`처럼 버전을 붙여 새로 판다
- 커스텀 RemoteViews를 쓰면 Live Update(Now Bar) 승격이 안 된다. 카드와 칩은 **동시에 못 가진다**
- 포그라운드 서비스를 멈추면 알림도 사라진다. 남기려면 `STOP_FOREGROUND_DETACH` + 재`notify`

---

## 2026-08-28 (회사) 1.3.37

### 한 일

기능을 더 넣음. ICS·커버·TalkBack은 그대로 제외.

1. **순위 카드** — 연승·최근 5, 현재 페이스 최종 승패, 자력 N승, 잔여 홈/원정, 매직 상대와 맞대결, 연전(예: `9/1~3 홈 두산 3연전`), 다음 5경기 시각, 텍스트 공유
2. **라이브·위젯** — 경기까지 카운트다운(`2시간 14분 후` / `D-2`). 위젯 레이스 줄에도 붙음
3. **워치** — 같은 레이스 줄(카운트다운 포함) 동기

### 확인함

- MagicNumber 단위 테스트 통과 (debug). 릴리스 `mergeReleaseResources`는 NAS 잠금으로 스킵. 기기 ADB 없음(설치는 다음에).

---

## 2026-08-28 (회사) 1.3.36

### 한 일

제안 10가지 중 7(ICS)·8(커버)·10(TalkBack) 제외하고 반영. 가을야구 컷 문구, 잔여 상대, 위젯 순위·잔여·선발, 매직/트래직 변동 알림, 워치 DataMap 순위.

### 확인함

- 단위 테스트 통과 (MagicNumber 9 + AlertPolicy 8 포함)
- S26 울트라에 1.3.36 설치. 워치 ADB는 없음. Now Bar 채널 v2(HIGH)는 기기에 있음. 경기 중이 아니라 칩 실기는 다음에.

---

## 2026-08-28 (회사) 1.3.35

### 한 일

순위 카드 탈락 숫자를 야구 용어 **트래직넘버**로 표기. `5위 트래직넘버 N (5위 승+롯데 패)`.

### 확인함

- S26 울트라에 1.3.35 설치 (08-28)

---

## 2026-08-27 (회사) 1.3.34

### 한 일

순위 카드 **탈락까지 N**이 남은 경기처럼 읽혀서 **`5위 탈락매직 N (5위 승+롯데 패)`** 로 바꿈.

---

## 2026-08-27 (회사) 1.3.33

### 한 일

워치는 나중에. **One UI 9 Now Bar 승격** + 순위 매직넘버 + 온보딩 안내.

1. **Now Bar** — 채널을 `live_score_nowbar_v2` / HIGH로 새로 팜(기존 DEFAULT는 코드로 못 올림). `setColorized(false)`, 칩 7자, `hasPromotableCharacteristics` 상태를 설정에 표시. 알림에서 칩 숨기기. 라이브 알림 설정 인텐트.
2. **순위** — 롯데 잔여 경기, 5위 확정 매직(또는 탈락까지), 1위 게임차/매직.
3. **온보딩** — Now Bar(라이브 알림) 켜는 경로. 이미 온보딩 본 기기는 Now Bar가 꺼져 있으면 한 번 더 안내.

### 아직 기기에서만 되는 것

- [ ] Now Bar 칩 실기 (S26 울트라, One UI 9)
- [ ] 갤럭시 웨어러블로 워치 앱 설치 (이번엔 보류)

---

## 2026-08-27 (회사)

### 한 일

1. **득점 알림** — `전준우 2점`처럼 애매하지 않게 **누가 + 어떤 타구 + 타점**. 타석 제목을 우선하고, `김민`이 `김민석`에 걸리는 매칭도 고침. 실점은 `실점! 김도영 적시타 · 1점`으로 투수가 준 점수처럼 안 읽히게.
2. **득점권 알림** — 제목에 `전준우 좌전 2루타, 득점권 · 타석 윤동희`. 본문에 주자 이름·타석·이닝 아웃. 이미 득점권인데 1루만 채워져도 다시 안 울림.
3. **라이트 라이브 히어로** — 다크처럼 위·아래가 한 톤. 검은 점수판 섬을 없애고 크림 배경으로 녹임. 큰 점수는 유지.

---

## 2026-08-26 (회사) 1.3.31

### 한 일

P0 버그 전부 + P1 기능 전부. 데이터는 **한 번만** 받고 바로 화면·위젯·워치에 반영.

1. **폴링 합침** — `refreshSnapshot()` single-flight + 8초 재사용. 앱 10초 폴링은 유지, 서비스/위젯은 방금 받은 스냅샷 재사용. 당겨서 새로고침은 강제 fetch.
2. **네트워크 실패 UX** — 이전 스냅샷 유지 + `마지막 HH:mm`. 타이틀·결과 빈 화면에 다시 시도.
3. **위젯 새로고침 → 워치 동기화**, 핫/콜드 존 스냅샷 연결.
4. **권한** — 알림 거부 안내. 설정에 정확한 알람·Now Bar·워치 마지막 동기화.
5. **알림 프리셋 / 무음 시간 / 경기 중에만** (`AlertPolicy` 테스트).
6. **점수 공유 이미지 + `sajik://game/{id}` 딥링크**, 타이틀 선수 검색 → 즐겨찾기.
7. **위젯 투명도·상대 로고**, DH 2차전 알람을 경기마다 다른 requestCode.

### 확인함 (08-27 이어서)

- 단위 테스트 17개 통과 (`AlertPolicy` 7 + 역전/득점 6 + 중계 그룹 4)
- 릴리스 APK 빌드 성공 (`1.3.31` / `1038`)
- ADB에 폰이 없어 실기 설치는 못 함

### 아직 기기에서만 되는 것

- [ ] Now Bar 칩 실기
- [ ] 갤럭시 웨어러블로 워치 앱 설치
- [ ] 폴드·플립 커버/플렉스

---

## 2026-08-26 (회사)

### 한 일

1. **레이아웃 전면 개편 (1.3.28)**  
   색만 바꾸던 단계에서 구조 교체. 점수판이 화면 맨 위 히어로, 상세는 세그먼트 탭, 하단은 떠 있는 아일랜드, 빨간 알약 칩·세로 막대 제목 제거. 결과/순위도 경기 행 톤.

2. **Now Bar (1.3.29 → 1.3.30)**  
   지원 폰에서 점수 칩. 처음엔 「라이브 바」만 승격됐고, **상세 모드 커스텀 알림이 칩을 막아** 세 모드 모두 ProgressStyle + 승격으로 고침. 칩 문구는 `3:2` (경기 전이면 시작 시각).

3. **Wear OS / 갤럭시 워치4 44mm (1.3.29)**  
   워치 패키지가 달라 점수가 안 넘어가던 문제 수정. 폰·워치 `applicationId` = `com.bossxor.lottegiants`. 폰 APK에 워치 APK 포함. 워치 앱·타일·컴플리케이션·경기 중 온고잉.

4. **작업일지**를 저장소에 추가 (이 파일).

### 확인함

- 단위 테스트(역전/득점 문구, 중계 그룹, 알림→중계 탭) 통과
- 릴리스 APK 빌드·S26 설치 성공
- 워치는 기기가 ADB에 없어 직접 설치는 못 함

### 집에 가서 이어서 하면 좋은 것

- [ ] Now Bar: 설정에서 라이브 바·상세·점수만 각각 고른 뒤 칩에 점수가 보이는지
- [ ] 갤럭시 웨어러블에서 워치 앱 설치, 타일/컴플리케이션에 점수
- [ ] 폴드·플립은 전용 레이아웃 없음. 펼친 화면은 동작, 커버·플렉스는 미검증
- [ ] 상세 알림은 이제 커스텀 카드가 아님(칩 때문에). 알림 본문만 더 김

---

## 유지해야 할 기능 (회귀 금지)

위젯·메인·알림은 **항상 롯데**. 결과에서만 다른 팀 시즌.

- 결과에서 팀 고르면 그 팀 시즌만. 엔트리/타이틀은 보고 있는 팀
- 결과에서 다른 팀 보다가 **하단 라이브 탭** → 롯데로
- 팀 로고·선수 얼굴 (네이버 CDN Referer)
- 중계: 아웃카운트·타석으로 묶기. 무안타는 안타 아님
- 역전 알림: `0:0 → 0:1`은 역전 아님. **지고 있던 팀이 앞설 때만** (`3:4 → 5:4`)
- 득점 알림: 누가 어떤 타구로 몇 타점 (`롯데 득점! 전준우 좌전 적시타 · 2타점 · 5:4`)
- 득점권 알림: 누가 어떻게 (`전준우 좌전 2루타, 득점권 · 타석 윤동희`)
- 알림 탭 → 해당 경기 **중계** 탭
- 요약 탭 루타 하이라이트(있으면 영상)
- 설정 즐겨찾기 **등판** 알림

---

## 최근 버전

| 버전 | code | 내용 |
|------|------|------|
| 1.3.45 | 1052 | 표시 모드 3개 분리, 종료 알림 지우기·자동 만료, 주요 장면 `0회`, 탭 접힘 완성 |
| 1.3.44 | 1051 | 스코어카드 colorized 채널, 라이브 상세 sticky 탭 |
| 1.3.42 | 1049 | 스코어카드 가로 채움, 칩 숨기기 제거 |
| 1.3.39 | 1046 | 로고·승률바 알림 카드, 승률 보정, 알림 지연 단축 |
| 1.3.38 | 1045 | 취소 경기 알림·위젯 0:0 차단 |
| 1.3.37 | 1044 | 순위 자력·페이스·연전, 위젯 카운트다운 |
| 1.3.36 | 1043 | 가을야구 컷·잔여 상대·위젯 한 줄·매직 알림 |
| 1.3.34 | 1041 | 탈락 숫자 설명 (5위 승+롯데 패) |
| 1.3.33 | 1040 | Now Bar 승격, 순위 매직·잔여, 온보딩 |
| 1.3.32 | 1039 | 득점·득점권 알림에 누가/어떻게가 보이게 |
| 1.3.31 | 1038 | P0/P1: 폴링 합침, 실패 UX, 알림 프리셋, 공유 이미지, 위젯 설정, DH 알람 |
| 1.3.30 | 1037 | Now Bar 칩 — 세 표시 모드 모두 |
| 1.3.29 | 1036 | Now Bar 승격 + 워치4 동반 앱 |
| 1.3.28 | 1035 | 레이아웃 전면 개편 |
| 1.3.27 | 1034 | 역전/득점 알림, 딥링크, 하이라이트, 즐겨찾기 등판 |

그 이전: 결과 팀 필터, 라이브 탭→롯데, 로고/얼굴, 다른 구장 상세, 인앱 업데이트 등. `git log` 참고.

---

## 작업 환경 메모

- 회사 PC: 워크스페이스가 네트워크 경로면 Gradle은 **`L:\` subst** 필수 (`Z:\` incremental Kotlin이 깨짐)
- `.\scripts\env.ps1` 로 JDK 17 / Android SDK. JDK는 하드코딩하지 않고 자동 탐색한다 (패치 버전이 올라가면 폴더 이름이 바뀜)
- 집 PC: `C:\Users\OJT\Downloads\App\Lotte widget`, JDK `C:\Program Files\Microsoft\jdk-17.x-hotspot`, SDK `C:\Android\Sdk`
- 설치: `adb -s R5KL10FHTTN install -r app\build\outputs\apk\release\app-release.apk`
- 푸시: GitHub private. credential 창이 멈추면 `gh auth token`으로 HTTPS push
- `.github/workflows` 변경은 OAuth `workflow` 스코프 없으면 푸시 거부될 수 있음
- 키스토어는 git에 없음. 집 PC에도 `keystore.properties` + `lotte-release.jks`가 있어야 릴리스 서명·인앱 업데이트가 이어짐

### 새 항목 템플릿 (복사해서 맨 위에)

```markdown
## YYYY-MM-DD (집 또는 회사)

### 한 일
-

### 확인함
-

### 다음에
- [ ]
```

---

## 추천 백로그 (2026-08-26 코드 전체 점검)

집·회사에서 이 체크리스트를 같이 쓴다.

### P0 — 지금 고치면 체감 큼
- [ ] 앱+서비스 10초 폴링 중복 제거
- [ ] 네트워크 실패 UX (마지막 갱신·다시 시도). Leaders 재시도 연결
- [ ] Now Bar 세 모드·워치4 타일 실기 확인
- [ ] 알림/Now Bar/정확 알람 권한 거부 시 안내
- [ ] 위젯 새로고침 때 워치도 동기화
- [ ] 스냅샷 hotColdZone 빈 리스트 고정 버그

### P1 — 넣으면 좋아짐
- [ ] 알림 프리셋 + 무음 시간대 + 경기 중에만
- [ ] 점수 공유 이미지·딥링크
- [ ] 선수 검색 (즐겨찾기 추가)
- [ ] 위젯 투명도·표시 항목
- [ ] DH 2차전 프리게임 알람
- [ ] EventDetector 단위 테스트

### P2 — 여유 있을 때
폴드 2단, TalkBack 설명, 온보딩에 Now Bar/워치, 플레이오프, ICS, 크래시 리포트

### 넣지 않는 게 나은 것
다른 팀 위젯/알림을 기본으로, 네이버급 풀 중계·VOD·티켓. 결과는 이미 전 구단 가능.
