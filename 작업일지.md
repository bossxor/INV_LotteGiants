# 집관 작업일지

집·회사에서 **이 파일만** 보고 이어서 작업한다.  
최신 **날짜**가 위에 오고, 같은 날이면 **버전**만 그 안에 위에 추가한다.

```powershell
git pull origin main
# 이 파일 수정
git add 작업일지.md
git commit -m "작업일지: (한 줄 요약)"
git push origin HEAD
```

버전은 `app/build.gradle.kts`만 올린다. APK·`.gradle`·`app/build`는 커밋하지 않는다.

---

## 현재 상태 (한눈에)

| 항목 | 값 |
|------|-----|
| 버전 | **2.0.4** / `versionCode` **2004** |
| 브랜치 | `main` |
| 백업 | `v1.3.87` · `backup/pre-multi-team-1.3.87` (2.0.0 개편 직전). 레이아웃 백업은 `backup/pre-redesign-1.3.27` |
| 실기 | S26 울트라(`R5KL10FHTTN`)에 **2.0.4** / 2004 설치됨 |
| 빌드 | `.\scripts\env.ps1` → `.\gradlew.bat :app:assembleDebug` (JDK 17 자동 탐색) |

---

## 2026-09-10 (회사)

### 2.0.4

1. **라이브·위젯 공백** — 오늘 KT-롯데가 있는데도 상세 보강이 실패하면 스냅샷 전체를 버려 라이브·위젯이 같이 비었다. 일정은 남기고 보강만 실패 처리.
2. **KBO 경기 전** — `GAME_TB_SC: null` 같은 필드를 빈 문자열로 받도록 함.
3. **실기 설치** — 로컬 릴리스 APK를 `install -r`로 덮어씀 (`versionCode=2004`).

### 2.0.3

1. **위젯** — 새로고침 버튼은 그대로 두고, 순위를 로고 아래로 옮기고 오른쪽 여백을 줘서 겹치지 않게 함.
2. **라이브** — 스냅샷이 없을 때 조회가 실패하면 스피너만 돌지 않고 다시 시도 안내. 휴일 워터마크·스코어 티커가 내 팀을 따름.
3. **실기 설치** — 로컬 릴리스 APK를 `install -r`로 덮어씀 (`versionCode=2003`).

### 2.0.2

1. **내 팀 전환** — 설정 맨 위에 10구단 선택. 기본값은 롯데라 기존 사용자는 그대로.
2. **데이터** — `my_team_code` · `LiveSnapshot.myTeamCode` · `TeamSwitcher`. 전환 시 스냅샷·중복알림 키를 지우고 알람·위젯·런처를 다시 맞춤.
3. **화면·알림·위젯** — 로고·문구·테마 강조색·워터마크·결과 탭 기본 필터가 내 팀을 따라감. 알림 설명은 팀명.
4. **보강** — 온보딩 안내, 즐겨찾기 안내, 백업 복원 후 런처 동기화, Keubo 팀카드 실패 시 빈 카드, Wear 소스 잔여 정리, 9팀 히스토리.
5. **배포 게이트** — `:app:testDebugUnitTest` 통과 후에만 커밋.
6. **실기 설치** — CI `latest` APK를 `install -r`로 덮어씀 (`versionCode=2002`).

### 2.0.1

1. **위젯 글자** — 선발 투수·잔여 경기를 한 단계 키움 (컴팩트 9/8→11/10, 와이드 11→13).
2. **구장명** — 예정·라이브·종료·취소 위젯에 사직·잠실 등 표시. 컴팩트에도 넣음.
3. **실기 설치** — CI `latest` APK를 `install -r`로 덮어씀.

### 2.0.0

1. **앱 이름** — 고정 자리(설정·알림 헤더)는 **집관**. 홈 화면은 `집관 사직`처럼 구장명.
2. **런처 아이콘** — 10구단 구장 배지. 배너 색은 팀 컬러, 글자는 홈구장. `activity-alias`로 전환하고, 켤 alias를 먼저 켠 뒤 나머지를 끈다. 실제 적용은 `MainActivity.onStop`.
3. **위젯 추가 화면** — 네이비·「롯데 라이브」를 빼고 회색·「경기 로딩 중」.
4. **Wear** — 폰과 연결되지 않은 모듈을 삭제.
5. **버전** — `2.0.0` / `versionCode` **2000**. 업데이트 체커는 정수만 비교하므로 점프해도 된다.
6. **실기 설치** — CI `latest` APK를 `install -r`로 덮어씀. 런처는 `Launcher_LT` 하나만 보임. `adb pm enable`은 One UI가 막아 앱 코드로만 전환한다.

---

## 2026-09-08 (회사)

### 1.3.87

1. **등말소 감시 시작** — 아침부터 돌지 않고 **14:00~23:00**만 본다. 라인업 알림은 경기 6시간 전부터 그대로.
2. **중계 조회** — LIVE 폴링은 현재 이닝만. **중계 탭**을 열 때 지난 이닝을 합친다.
3. **루타** — 승률·하이라이트를 스냅샷마다 치지 않고 **25초**에 한 번.
4. **감시 간격** — 라인업 창이면 15초, 아니면 45초.
5. **APK** — `material-icons-extended` 제거. 퀵링크·테마 아이콘은 core만.
6. **휴일 히어로** — 오늘 롯데 경기가 없으면 최근 결과와 다음 경기를 한눈에.
7. **진동** — 설정에서 득점·역전 알림 진동을 끌 수 있다.
8. **실기 설치** — CI `latest` APK를 `install -r`로 덮어씀. 1.3.86과 서명이 같아 삭제는 하지 않았다.

### 1.3.86

1. **시작이 무겁던 문제** — 열자마자 순위·타이틀·엔트리·월간 일정을 받던 것을 해당 탭에 들어갈 때만 받는다.
2. **폴링** — LIVE가 아니면 앱 자동 새로고침을 10초에서 45초로.
3. **감시 FGS** — 07–24시 상시가 아니라 등말소 08–23시 또는 라인업 창만.
4. **이미지** — Coil 메모리 12% · 디스크 40MB, 로고·사진을 표시 크기로만 디코드.
5. **설정** — 즐겨찾기·알림 JSON 백업/복원. 삼성 잠자기 앱 안내 버튼.
6. **순위** — 선수 이름 검색 후 ☆ 즐겨찾기.
7. **실기 설치** — USB 재연결 후 CI `latest` APK를 `install -r`로 덮어씀. 1.3.85와 서명이 같아 삭제는 하지 않았다.

### 1.3.85

1. **실시간 조회 오류·폰 차단** — 스냅샷이 8초마다 36일 일정·종료 경기 상세·Keubo 등말소를 같이 쳐서 API 실패가 나고, 실패하면 라이브 서비스가 멈춰 앱을 열어야 알림이 왔다. 오늘 경기만 자주 받고, 실패 시 마지막 스냅샷을 유지한다.
2. **라인업·등말소** — 감시 15초/25초. 알람과 겹치면 건너뛴다. 변동이 없어도 Keubo 전체를 받지 않는다.
3. **위젯** — 그리는 중에 네트워크를 치지 않고, 새로고침이 실패해도 마지막 스코어로 다시 그린다.
4. **실기 설치** — 회사 PC·폰에 깔려 있던 1.3.84 서명이 CI `latest`와도 달라 `install -r`이 거절됨. `adb uninstall` 후 `latest` APK(1.3.85 / 1093)로 재설치했다. 즐겨찾기·알림 설정은 다시 잡아야 한다.
5. **README** — 1.3.85 폴링·위젯 실패 처리, `latest` APK adb 설치, 서명 불일치 시 uninstall 경로를 적었다.

---

## 2026-09-07 (회사)

### 1.3.84

1. **README** — 버전·순위 화면·종료 알림·런처 아이콘·`L:`/`adb` 설치 경로를 1.3.84 기준으로 맞춤.

---

## 2026-09-06 (집)

### 1.3.84

1. **런처 아이콘** — SAJIK 배지가 홈 화면에서 과하게 커 보여, 전경에 16% inset을 넣어 참고 이미지처럼 여백이 있게 줄였다.

### 1.3.83

1. **종료 알림을 지워도 다시 뜨던 문제** — 오늘 종료·취소 경기를 앱·워커가 계속 다시 올렸다. 스와이프로 지운 경기 ID를 기억하고, `다시 표시`나 새 LIVE 전까지는 같은 결과 카드를 올리지 않는다.

---

## 2026-09-04 (회사)

### 1.3.82

1. **순위 상단** — 레이스 요약 · 잔여 상대 · 다음 5경기를 각각 따로 둔다.

### 1.3.81

1. **순위 카드** — 텍스트 나열을 줄이고, 잔여 상대는 로고 칩, 다음 5경기는 로고·시각·구장·홈/원정으로 나눈다. 시즌 트렌드 이하는 그대로.

### 1.3.80

1. **워치 APK** — 폰에 넣어도 워치에 안 올라가므로 `wearApp`·WearBridge·Play Services Wearable을 폰에서 뺀다.
2. **순위표** — 승률 왼쪽에 연속(연승·연패) 칸을 넣는다.

### 1.3.79

1. **라이브 바** — Now Bar 승격이 스코어카드를 지워서, 승격 경로를 빼고 상세와 같은 카드로 고정.
2. **죽은 코드** — ProgressStyle·삼성 extras·Now Bar 안내·레거시 `AllGamesScreen`·안 쓰는 저장소 API·fragment-ktx·미사용 리소스를 제거.

### 1.3.78

1. **라이브 바 디자인이 안 바뀌던 문제** — `setRequestPromotedOngoing`이 커스텀 스코어카드를 지웠다. 승격 플래그를 빼고 상세 알림과 같은 카드로 그린다.

### 1.3.77

1. **위젯** — 새로고침 누르면 금색. 컴팩트는 순위·로고를 가운데 크게.
2. **등말소 새벽 재알림** — 날짜만 바뀌거나 선수코드가 비었다 채워져도 같은 공시는 다시 안 울림.
3. **경기 전 알림** — 원정/홈·구장 표시. 같은 내용이면 프로세스 넘어가도 다시 안 그림.
4. **라이브 다이아** — 52dp.
5. **점수만** — 로고와 점수만 있는 스코어카드.
6. **라이브 바** — 상세 알림과 같은 스코어카드.

---

## 2026-09-03 (회사)

### 1.3.76

1. **위젯 새로고침이 안 눌리던 문제** — Glance `ActionCallback`이 삼성 홈에서 씹히고, 본문 클릭과 겹쳤다. 버튼을 오른쪽 위 오버레이로 두고 위젯 리시버 브로드캐스트로 강제 fetch.
2. **내용이 아래로 쏠리던 문제** — 새로고침 전용 줄이 본문을 밀고, 컴팩트 헤더 아래 `defaultWeight` 빈칸이 스코어를 내렸다. 버튼을 겹치고 점수는 제목 바로 아래로.

### 1.3.75

1. **라이브 히어로가 어제 결과** — 위젯만 날짜를 걸렀고 라이브 탭은 `lotteGame`을 그대로 썼다. 오전 5시 기준 오늘이 아니면 다음 예정으로 넘기고 스냅샷을 다시 받는다.
2. **히어로 디자인** — 앞선 점수 금색, 라이트 모드 빨간 그라데이션을 되돌림. 테마 칩은 바로 선택되고 상태바 아이콘도 테마를 따라간다.

### 1.3.74

1. **어제 결과가 위젯·라이브에 남던 문제** — `kboToday()`는 오전 5시 경계인데, 스냅샷이 어제 종료 경기를 계속 `lotteGame`으로 들고 있었다. 5시 이후 스냅샷은 버리고, 일정 API가 섞어 준 전날 경기도 걸러 낸다.
2. **위젯 초기화** — 앱 내부 업데이트(`MY_PACKAGE_REPLACED`)와 매일 05:00 알람에서 스냅샷을 강제 갱신한 뒤 위젯을 다시 그린다.

확인: 단위 테스트·debug 빌드.

### 1.3.73

1. **Now Bar = Ongoing Activity?** — 네이버지도 공지는 삼성 One UI 7 파트너 기능. 공개 SDK 없음, 패키지 화이트리스트.
2. S26은 One UI 8 Live Update가 Now Bar 경로. **라이브 바**를 커스텀 스코어카드에서 `ProgressStyle` + `setRequestPromotedOngoing`으로 되돌림. 상세 알림 카드는 유지.
3. 네이버지도와 같은 `com.samsung.android.support.ongoing_activity` 메타데이터 + `android.ongoingActivityNoti.*` extras를 칩에 붙임 (화이트리스트 없이는 무시될 수 있음).
4. Now Bar 채널 `live_score_nowbar_v2`를 만들자마자 지우던 버그 수정.

확인: 단위 테스트·debug 빌드. 칩 실기는 경기 중 + 설정에서 「라이브 바」.

### 1.3.72

1. **자동 업데이트 속도** — 앱 프로세스 시작과 동시에 GitHub를 본다. 릴리스 본문 `versionCode`로 최신 여부를 판단해 이미 최신이면 `update.json`을 받지 않는다.
2. 비공개 APK는 API 에셋+토큰으로 받는다. 「확인 중」 창은 없애고, 받을 때만 진행률.
3. 앱을 다시 켜면 45초 뒤부터 재확인.

확인: 단위 테스트·debug 빌드. S26 설치.

### 1.3.71

1. **위젯 새로고침** — 전체 영역 클릭이 버튼을 가로채던 문제를 고침. 누르면 강제 fetch.
2. **새로고침 실패 배너** — 캐시가 있으면 자동 폴링 실패로 다시 띄우지 않음. 닫아도 10초마다 돌아오던 원인.
3. **승률 1:99** — 3회 2점차처럼 초반 API 극단값을 점수 추정 근처로 보정.
4. **알림 배치** — 빅이닝식 로고–점수–다이아–점수–로고 / 투수·BSO·타자 / 승률 바.

확인: 단위 테스트·debug 빌드 후 S26 설치.

---

## 2026-09-02 (회사)

### 1.3.70

1. **「새로고침 실패 · 마지막 HH:mm」** — 히스토리·엔트리가 아니라 라이브 점수 자동 새로고침 안내임을 분리. 닫기 추가.

### 1.3.69

1. 상태표시줄 야구공 실루엣, 잠금화면 `publicVersion`·칩 문구.

### 1.3.68

1. Now Bar 깜빡임: 경기 전 FGS 반복 시작 제거, 동일 내용 notify 생략.

### 1.3.67

1. 런처 아이콘을 SAJIK 구장 배지로 교체.

### 1.3.66

1. 라이브 바·점수만도 스코어카드. `CATEGORY_PROGRESS`가 ProgressStyle로 덮이던 문제.

### 1.3.65

1. **상세 알림 다시 표시** — FGS가 메인 스레드에서 로고를 받으며 알림을 지우던 문제 수정. 경기 전에는 알림만 고정.
2. **라이브 바·점수만** — 스코어카드 디자인 통일 (캐시 로고만 사용).

확인: 단위 테스트·debug 빌드 후 S26 설치.

### 1.3.64

1. **다시 표시** — lead 창 밖 경기 전에도 알림 고정(pinned). FAST_POLL이 서비스를 켜며 알림을 지우던 문제 수정.
2. **라이브 바** — 옛 이닝 진행 바(ProgressStyle) 대신 스코어카드 디자인 사용.

확인: 단위 테스트·debug 빌드 후 S26 설치.

### 1.3.63

1. **앱 안 열면 알림 안 오던 문제** — WorkManager(15분)만 기대하던 구조를 고침. `AlertWatchService` 포그라운드 20초 폴링, 부팅 시 `AlertBootstrap` 즉시 1회 + fast poll 예약, `ROSTER_POLL` 매니페스트 등록, WakeLock.
2. **30분 늦음** — 삼성 배터리 최적화에서 Alarm이 끊겨도 FGS가 KBO GetRoster·라인업을 직접 본다.
3. **README** — 엔트리·라인업 3중 백업 구조 문서화.

확인: 단위 테스트·debug 빌드 후 S26 설치.

### 1.3.62

1. **엔트리·라인업 알림 속도** — KBO 공식 GetRoster 경량 폴링(30초, 08~23시). 라인업 fast poll 3분→20초(경기 6시간 전~). 경기 전 LiveScoreService 20초→8초.
2. **README** — 버전 1.3.62 반영.

확인: 단위 테스트·debug 빌드 후 S26 울트라 설치.

### 1.3.61

1. **승리확률 0:0 버그** — 네이버 `1`/`99` 퍼센트를 100%/99%로 잘못 읽던 파싱 수정. 0:0이어도 40:60 같은 값은 유지. 네이버 중계 → 루타 → 추정 순으로 소스 선택.
2. **hotColdZone** — 프리뷰 로딩 전 빈 스냅샷이 고정되던 것을 이전 값 유지로 수정.
3. **네트워크 실패 UX** — 라이브 화면 오류 줄에 `다시 시도` 버튼.
4. **README** — 버전 표를 1.3.61에 맞춤.
5. **단위 테스트** — `WinProbTest` 추가.

### 1.3.60

1. **실시간 스코어 알림 다시 표시** — 표시 시작 시간과 무관하게 오늘·다음 경기 알림을 바로 띄움.
2. **Now Bar 설정 버튼** — 문구 정리, 열리면 라이브 알림 경로 토스트 안내.

확인: debug 빌드 후 S26 설치.

### 1.3.59

1. **Now Bar 표시 시작** — 최대 4시간, 30분 단위 칩 8개로 고침 (슬라이더 3시간 안 잡히던 문제).
2. **설정 문구** — 표시 시작 아래 설명 줄임.
3. **알림 프리셋** — 무음 시간 UI 제거.

확인: 단위 테스트·debug 빌드 후 S26 설치.

### 1.3.58

1. **Now Bar 시작** — 설정에서 경기 시작 몇 분 전부터 띄울지 고름 (30분 단위, 기본 2시간). 아침부터 하루 종일 안 뜸.
2. **종료 후** — 결과 카드는 남기되 `ongoing`을 떼서 밀어 지울 수 있게. 다음 경기 카드로 안 바뀜.
3. **펼친 알림** — 루상 다이아몬드·BSO 점을 키움.

확인: 단위 테스트·debug 빌드 후 S26 울트라 설치.

---

## 2026-09-01 (회사)

### 1.3.57

알림을 누르면 메인만 열리던 것을 고침. 득점→중계, 라인업→라인업 탭, 엔트리 등말소→엔트리 화면. `sajik://` 딥링크로 PendingIntent extras가 비는 것도 막음.

확인: 단위 테스트·debug 빌드 후 S26 울트라 설치.

### 1.3.56

펼친 상세 알림이 밋밋하고 답답했다. 네이비 박스는 다시 안 넣고, 배치·대비만 손봤다.

1. **배치** — 이닝 알약을 점수 사이에서 위로. 가운데는 `3 : 2`. 루상+BSO는 한 줄.
2. **색** — 빈 루·BSO 점이 네이비용 흰색이라 시스템 알림에서 묻히던 걸 중간 회색으로. 알약은 라이브=초록 / 예정·중단=골드 / 종료=회색 / 취소=레드. 승률 바 끝을 둥글게.

확인: debug 빌드 후 S26 울트라 설치. 알림 펼쳐서 확인.

### 1.3.55

1. **우천중단** — 취소와 구분. `우천중단 (19:50 예정)`처럼 사유+재개 시각. 상태는 LIVE 유지(점수·폴링·알림 계속).
2. **라이브 히어로** — 빅이닝 때 넣었던 흰 카드·팀별 박스를 빼서 한 덩어리로.

확인: 단위 테스트·debug 빌드 후 S26 울트라 설치.

### 1.3.54

`README.md`를 1.3.54 기준으로 맞춤. 라이브 히어로·위젯 순위·Now Bar 조건·`env.ps1`/`L:\` 빌드.

위젯 하단이 `7위 · 잔여 31 · 선발 로드리게스 · 8...`로 잘렸다. 순위는 각 로고 위, 선발은 그 위 줄만, 하단은 `잔여 31`만.

확인: S26 울트라에 1.3.54 debug 설치. 위젯 새로고침 후 확인.

### 1.3.53

1. **위젯** — 1.3.38 취소 전용 레이아웃을 빼고 그 직전(1.3.37)으로 되돌림. 캐시가 비어 있으면 한 번 fetch.
2. **폴링** — 서비스가 `force=true`로 앱과 같이 받아 가던 것을 `force=false` + 8초 캐시 재사용.
3. **README** — Now Bar 칩은 라이브 바·점수만. 상세 알림은 승격 안 됨.

하단 탭 여백 80dp는 안 넣음. Scaffold가 이미 bottomBar 높이를 뺀다.

확인: S26 울트라에 1.3.53 debug 설치 (`adb install -r`). 서명 충돌 없음.

---

## 2026-08-31 (회사)

### 1.3.52

[빅이닝](https://blog.naver.com/lifebystride/224316526720) 라이브 카드 느낌을 가져왔다. 기능은 그대로 두고 비주얼만.

1. **라이브 히어로** — 흰 둥근 카드, 초록 LIVE / WIN·LOSE·DRAW, 가운데 `0 : 0`, 초록 이닝 알약, 팀 카드(로고·이름·선발), 경기 중 다이아몬드+BSO+투수/타자.
2. **오늘 경기 줄** — 가로 칩이 아니라 카드(LIVE 알약, 큰 점수).
3. **일정 캘린더** — 예정 홈=파랑, 원정=빨강.

마스코트 일러스트·응원 하트는 안 넣음. 공식 로고를 쓴다.

확인: `:app:assembleDebug` 성공. ADB에 기기 없음.

### 1.3.51

하단 탭에서 선택된 페이지가 점만 보여서 이름을 알기 어려웠다. 라이브·결과·순위·설정 **항상 글자로 표시**, 선택된 탭은 볼드·롯데 레드.

확인: S26 울트라에 1.3.51 설치. 기존 1.3.49와 서명이 달라 앱을 지운 뒤 다시 깔음.

### 1.3.50

1. **다시 표시가 예전 ProgressStyle로 뜨던 것** — 오늘 경기가 없으면 `lotteGame`이 null이라 점선 바로 떨어졌다. 다음 경기를 쓰고, 스냅샷을 먼저 받은 뒤 카드를 올린다. 서비스를 껐다 켜서 기본 알림이 깜빡이지 않게 같은 ID로 덮는다.
2. **승률 게이지** — 펼친 카드 하단에 `LG 42%` / `58% 롯데`와 색 바. 어두운 팀색은 알림 배경에 묻히지 않게 밝힘. 취소만 숨김.
3. **매직·트래직 알림 사유** — `12 → 11 · 롯데, 두산 5:3 승 · KT, 키움 2:4 패` 처럼 당일·어제 경기 결과를 본문에 넣음.

확인: MagicNumber 단위 테스트 통과 (사유 문구 포함).

---

## 2026-08-30 (집)

### 1.3.49

네이비 배경을 빼면 시스템 알림 회색과 한 덩어리가 된다. 로고·점수·상태 알약은 그대로 두고, 글자색은 `TextAppearance.Compat.Notification`로 다크/라이트 셰이드를 따른다. colorized는 끈다(작은 아이콘만 롯데 레드).

확인: 접힌 알림은 시스템과 같은 회색 칸. 펼치면 헤더·내용이 한 색, `구장 사직` 보임.

### 1.3.48

작은/큰 알림 모두 바깥 회색이 남는 이유를 재확인했다. 안쪽 네이비는 우리 RemoteViews 배경이고, 왼쪽 아이콘·오른쪽 펼치기·헤더 회색은 One UI가 Android 12+ 표준 템플릿으로 강제하는 칸이다. `setColorized(true)`는 포그라운드 서비스이거나 MediaStyle+세션일 때만 시스템 배경을 칠하는데, 종료·취소는 서비스를 떼서 색이 무시되고, MediaStyle을 붙이면 스코어보드가 미디어 알림으로 바뀌어 못 쓴다. **네이비 스코어보드 박스 유지.**

### 1.3.47

`실시간 스코어 알림 다시 표시`를 누르면 경기 전이 예전 ProgressStyle로 나왔다. 상세 알림 카드가 라이브·종료에만 붙고 `BEFORE`는 빠져 있었기 때문.

1. 상세 알림(FULL)이면 경기 전·중·종료·취소 모두 네이비 카드.
2. 경기 전 카드: 가운데 알약에 시작 시각(`18:00`), 펼치면 양 선발, 접히면 구장. 승률 바는 숨김.
3. 다시 표시 직후 빈 알림이 잠깐 예전 디자인으로 뜨지 않게, 서비스가 캐시된 스냅샷으로 바로 카드를 올린다.

확인: 채널 `live_score_card_v3`. 접힌 카드에 로고·점수·취소 알약. 펼치면 구장만.

### 1.3.46

알림이 "일부 잘려 보인다"는 지적을 좌표로 재봤다. **좌측 여백은 One UI가 모든 알림에 잡는 앱 아이콘 칸**이고, 우리 카드는 그 오른쪽을 이미 100% 쓰고 있었다. 앱에서 더 넓힐 방법은 없다. 대신 **내용이 잘리는 것**을 고쳤다.

1. **접힌 알림 전용 한 줄 레이아웃** (`notification_live_compact.xml`) — 로고·점수·상태 알약만. 큰 카드는 펼친 상태·`BigContentView`에만.
2. **투수/타자 이름 안 잘리게** — BSO를 가운데 독립 줄로 올리고 투수·타자에 각각 절반 폭.
3. 종료 상태 접힌 줄에서는 승패 문구를 빼서 잘림 없음.

확인: 접힌 알림 `LG 8 [종료] 3 롯데` 한 줄. 가용 영역 전체 사용.

다음에: 경기 중 카드(투구수·루상·BSO·타자)는 다음 경기 때 실기 확인.

### 1.3.45

집에서 1.3.39부터 1.3.44까지 몰아 넣은 것들을 다시 보니 **모드가 겹치고 알림이 안 지워지는** 문제가 있어서 정리했다.

1. **표시 모드 3개가 실제로 달라짐** — `mode == FULL`일 때만 카드를 쓴다. 라이브 바=ProgressStyle+칩, 상세=스코어카드, 점수만=한 줄+칩.
2. **끝난 경기 알림이 안 없어지던 것** — 종료·취소면 `setOngoing(false)` + 2시간 후 자동 만료.
3. **새벽에 어제 경기 카드가 다시 뜨던 것** — **오늘 날짜 경기만** 올린다.
4. **종료 카드 내용** — 빈 다이아몬드·BSO를 숨기고 `승/패` 투수.
5. **요약 탭 주요 장면 `0회`** — 이닝이 없으면 칸을 비운다.
6. **라이브 탭 접힘 완성** — 탭 내용이 짧아도 한 화면을 채움.
7. **`scripts\env.ps1`** — JDK 17 자동 탐색 (집 PC 경로가 달라도 빌드).
8. 안 쓰는 `notification_live_big.xml` 삭제.

확인 (S26 울트라): 릴리스 설치. 라이브 탭 접힘, 주요 장면 `0회` 사라짐, 상세 알림 스와이프로 지워짐.

---

## 2026-08-29 (집)

### 1.3.44

스코어카드 채널을 `live_score_card_v3`(DEFAULT)로 올려 `setColorized`가 먹게. 라이브 상세를 `LazyColumn` + `stickyHeader`로.

### 1.3.43

종료 경기 알림에서 남은 주자·볼카운트 제거(`liveSituation`), 빈 루 드로어블은 테두리만. 라이브 탭 스크롤 시 상단 접힘 1차.

### 1.3.42

스코어카드가 알림창 가로를 채우게 `DecoratedCustomViewStyle` 제거, 「칩 숨기기」 버튼 삭제. *이때 지울 방법이 사라졌다 (1.3.45에서 고침).*

### 1.3.41

라이브 탭 히스토리·엔트리·타이틀을 아이콘 카드로 (텍스트만인 링크 제거).

### 1.3.40

실시간 알림을 점수판 카드로 강제, 투수 **투구수** 표시. *이때 라이브 바 모드가 카드에 덮였다 (1.3.45에서 고침).*

### 1.3.39

상세 알림을 **로고·승률바 카드**로. 요약 승률(네이버 한쪽만 와도 보정 + 이전 스냅샷 누적), 경기 90분 전부터 폴링·3분 검사로 라인업·취소·등말소 지연 단축.

### 1.3.38

취소 경기에 30분 전 알림·위젯 `0:0` 스코어보드가 뜨지 않게. 취소 판별 강화 + 예약 알람 해제, 위젯은 취소 라벨만.

이때 배운 것: `setColorized`는 채널 DEFAULT 이상. 채널 중요도는 ID를 바꿔 새로 판다. 커스텀 RemoteViews면 Now Bar 승격이 안 된다. FGS를 멈추면 알림도 사라지므로 `STOP_FOREGROUND_DETACH` + 재`notify`.

---

## 2026-08-28 (회사)

### 1.3.37

기능을 더 넣음. ICS·커버·TalkBack은 그대로 제외.

1. **순위 카드** — 연승·최근 5, 현재 페이스 최종 승패, 자력 N승, 잔여 홈/원정, 매직 상대와 맞대결, 연전, 다음 5경기 시각, 텍스트 공유.
2. **라이브·위젯** — 경기까지 카운트다운. 위젯 레이스 줄에도 붙음.
3. **워치** — 같은 레이스 줄(카운트다운 포함) 동기.

확인: MagicNumber 단위 테스트 통과. 릴리스 `mergeReleaseResources`는 NAS 잠금으로 스킵.

### 1.3.36

제안 10가지 중 7(ICS)·8(커버)·10(TalkBack) 제외하고 반영. 가을야구 컷 문구, 잔여 상대, 위젯 순위·잔여·선발, 매직/트래직 변동 알림, 워치 DataMap 순위.

확인: 단위 테스트 통과. S26에 1.3.36 설치.

### 1.3.35

순위 카드 탈락 숫자를 야구 용어 **트래직넘버**로 표기. `5위 트래직넘버 N (5위 승+롯데 패)`.

확인: S26 울트라에 1.3.35 설치.

---

## 2026-08-27 (회사)

### 1.3.34

순위 카드 **탈락까지 N**이 남은 경기처럼 읽혀서 **`5위 탈락매직 N (5위 승+롯데 패)`** 로 바꿈.

### 1.3.33

워치는 나중에. **One UI 9 Now Bar 승격** + 순위 매직넘버 + 온보딩 안내.

1. **Now Bar** — 채널을 `live_score_nowbar_v2` / HIGH로 새로 팜. `setColorized(false)`, 칩 7자, 알림에서 칩 숨기기.
2. **순위** — 롯데 잔여 경기, 5위 확정 매직(또는 탈락까지), 1위 게임차/매직.
3. **온보딩** — Now Bar(라이브 알림) 켜는 경로.

### 1.3.32

1. **득점 알림** — 누가 + 어떤 타구 + 타점. `김민`이 `김민석`에 걸리는 매칭도 고침. 실점은 투수가 준 점수처럼 안 읽히게.
2. **득점권 알림** — 제목에 `전준우 좌전 2루타, 득점권 · 타석 윤동희`. 이미 득점권인데 1루만 채워져도 다시 안 울림.
3. **라이트 라이브 히어로** — 검은 점수판 섬을 없애고 크림 배경으로 녹임.

---

## 2026-08-26 (회사)

### 1.3.31

P0 버그 전부 + P1 기능 전부. 데이터는 **한 번만** 받고 바로 화면·위젯·워치에 반영.

1. **폴링 합침** — `refreshSnapshot()` single-flight + 8초 재사용.
2. **네트워크 실패 UX** — 이전 스냅샷 유지 + `마지막 HH:mm`.
3. **위젯 새로고침 → 워치 동기화**, 핫/콜드 존 스냅샷 연결.
4. **권한** — 알림 거부 안내. 설정에 정확한 알람·Now Bar·워치 마지막 동기화.
5. **알림 프리셋 / 무음 시간 / 경기 중에만**.
6. **점수 공유 이미지 + `sajik://game/{id}` 딥링크**.
7. **위젯 투명도·상대 로고**, DH 2차전 알람을 경기마다 다른 requestCode.

확인: 단위 테스트 17개 통과. 릴리스 APK `1.3.31` / `1038`. 당시 ADB에 폰 없음.

### 1.3.30

Now Bar 칩. 세 모드 모두 ProgressStyle + 승격. 칩 문구는 `3:2` (경기 전이면 시작 시각).

### 1.3.29

Now Bar 승격 + Wear OS / 갤럭시 워치4. 폰·워치 `applicationId` = `com.bossxor.lottegiants`. 폰 APK에 워치 APK 포함.

### 1.3.28

레이아웃 전면 개편. 점수판이 화면 맨 위 히어로, 상세는 세그먼트 탭, 하단은 떠 있는 아일랜드. 작업일지를 저장소에 추가.

확인: 단위 테스트 통과. 릴리스 APK·S26 설치.

---

## 유지해야 할 기능 (회귀 금지)

위젯·메인·알림은 **항상 롯데**. 결과에서만 다른 팀 시즌.

- 결과에서 팀 고르면 그 팀 시즌만. 엔트리/타이틀은 보고 있는 팀
- 결과에서 다른 팀 보다가 **하단 라이브 탭** → 롯데로
- 팀 로고·선수 얼굴 (네이버 CDN Referer)
- 중계: 아웃카운트·타석으로 묶기. 무안타는 안타 아님
- 역전 알림: `0:0 → 0:1`은 역전 아님. **지고 있던 팀이 앞설 때만** (`3:4 → 5:4`)
- 득점 알림: 누가 어떤 타구로 몇 타점 (`롯데 득점! 전준우 좌전 적시타 · 2타점 · 5:4`)
- 득점권 알림: 누가 어떻게 (`전준우 좌전 2루타, 득점권 · 타석 윤동희`)
- 알림 탭 → 해당 경기 **중계** 탭
- 요약 탭 루타 하이라이트(있으면 영상)
- 설정 즐겨찾기 **등판** 알림

---

## 최근 버전

| 버전 | code | 내용 |
|------|------|------|
| 1.3.87 | 1095 | 등말소 14시, LIVE 현재 이닝만, 루타 25초, 감시 45초, 휴일 히어로, 득점 진동 |
| 1.3.86 | 1094 | 탭 지연 로딩, 비경기 폴링 45초, 감시 FGS 축소, 설정 백업 |
| 1.3.85 | 1093 | 실시간 조회 과다 호출 완화, 라인업 15초·등말소 25초 |
| 1.3.84 | 1092 | 런처 SAJIK 배지 여백 |
| 1.3.83 | 1091 | 종료 알림을 지우면 다시 안 올림 |
| 1.3.45 | 1052 | 표시 모드 3개 분리, 종료 알림 지우기·자동 만료 |
| 1.3.31 | 1038 | 폴링 합침, 실패 UX, 알림 프리셋 |
| 1.3.28 | 1035 | 레이아웃 전면 개편 |

그 이전은 위 날짜 칸 또는 `git log`.

---

## 작업 환경 메모

- 회사 PC: 워크스페이스가 네트워크 경로면 Gradle은 **`L:\` subst** 필수
- `.\scripts\env.ps1` 로 JDK 17 / Android SDK. JDK는 하드코딩하지 않고 자동 탐색
- 집 PC: `C:\Users\OJT\Downloads\App\Lotte widget`, JDK `C:\Program Files\Microsoft\jdk-17.x-hotspot`, SDK `C:\Android\Sdk`
- 설치: `gh release download latest` 후 `%TEMP%` APK를 `adb -s R5KL10FHTTN install -r`. 서명 불일치면 uninstall 후 재설치
- 푸시: GitHub private. credential 창이 멈추면 `gh auth token`으로 HTTPS push
- 키스토어는 git에 없음. 집 PC에도 `keystore.properties` + `lotte-release.jks`가 있어야 함

### 새 항목 템플릿 (같은 날이면 버전만 추가)

```markdown
## YYYY-MM-DD (집 또는 회사)

### 1.x.xx

1. **제목** — 내용
```

---

## 추천 백로그 (2026-08-26 코드 전체 점검)

집·회사에서 이 체크리스트를 같이 쓴다. 이미 반영된 항목도 있어, 새 작업 전에 날짜 칸을 본다.

### P0 — 지금 고치면 체감 큼
- [ ] 앱+서비스 10초 폴링 중복 제거
- [ ] 네트워크 실패 UX (마지막 갱신·다시 시도). Leaders 재시도 연결
- [ ] Now Bar 세 모드·워치4 타일 실기 확인
- [ ] 알림/Now Bar/정확 알람 권한 거부 시 안내
- [ ] 위젯 새로고침 때 워치도 동기화
- [ ] 스냅샷 hotColdZone 빈 리스트 고정 버그

### P1 — 넣으면 좋아짐
- [ ] 알림 프리셋 + 무음 시간대 + 경기 중에만
- [ ] 점수 공유 이미지·딥링크
- [ ] 선수 검색 (즐겨찾기 추가)
- [ ] 위젯 투명도·표시 항목
- [ ] DH 2차전 프리게임 알람
- [ ] EventDetector 단위 테스트

### P2 — 여유 있을 때
폴드 2단, TalkBack 설명, 온보딩에 Now Bar/워치, 플레이오프, ICS, 크래시 리포트

### 넣지 않는 게 나은 것
다른 팀 위젯/알림을 기본으로, 네이버급 풀 중계·VOD·티켓. 결과는 이미 전 구단 가능.
